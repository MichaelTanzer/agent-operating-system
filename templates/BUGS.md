# Bugs

<!-- Known issues that exist but have been consciously deferred.
     This is NOT a bug tracker — it's a "known liabilities" register.
     Purpose: agents reading this know "if X happens, that's expected,
     don't try to fix it as a side-quest."

     Format for each entry:
     ---
     ## BUG-NNN — Short title

     **Status:** Open | Investigating | Fixed in <PR/commit>
     **Severity:** Critical | High | Medium | Low
     **Discovered:** YYYY-MM-DD
     **Symptoms:** What does the failure look like?
     **Workaround:** How to avoid or mitigate it for now.
     **Root cause (if known):** Brief explanation.
     **Fix plan:** What the fix will look like when we get to it.
     ---

     Rules:
     - Add a new entry when you find a bug you're NOT fixing immediately.
     - Update the Status field when a bug is fixed.
     - Never delete entries — mark them Fixed instead.
     - Agents: if you encounter a symptom listed here, DO NOT try to fix it
       unless it's your assigned task. Note it in your status report.
-->

## BUG-001 — (placeholder — replace with first real bug)

**Status:** Open
**Severity:** Low
**Discovered:** <!-- YYYY-MM-DD -->
**Symptoms:** <!-- what does failure look like? -->
**Workaround:** <!-- how to avoid triggering it -->
**Root cause:** <!-- known / unknown -->
**Fix plan:** <!-- when and how we'll fix it -->
